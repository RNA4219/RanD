---
acceptance_id: AC-20260528-02
task_id: TASK-20260528-02
intent_id: INT-KANO-EVAL-001
owner: rand
status: passed
reviewed_at: 2026-05-28
reviewed_by: pending
approval_type: technical
release_approval_id: null
---

# KanoMode Eval hardening acceptance

## Scope

- 対象変更:
  - audit overall verdict の `no_go` 伝播
  - discovery promotion gate の強化
  - discovery / audit golden fixture
  - CLI black-box Eval の acceptance 化
  - manual BB / Code-to-gate 再検収
- 非対象:
  - live web search 必須化
  - LLM provider による本格分類推論
  - downstream OSS 本体の変更
  - Kestra / Misskey / tracker 実送信

## Acceptance Criteria

- [x] `uv run pytest` が通る。
- [x] audit fixture に `no_go` がある場合、`requirements_audit_packet.gate_summary.overall_assessment` が `no_go` になる。
- [x] discovery fixture の低 confidence 候補が `requirements_packet` に昇格しない。
- [x] discovery fixture の `questionable` 候補が `requirements_packet` に昇格しない。
- [x] discovery fixture の `primary` / `user_signal` を持たない候補が `requirements_packet` に昇格しない。
- [x] attractive 候補が `P2` / `soft_experiment_gate` のまま維持される。
- [x] discovery / audit golden fixture が期待値を検証している。
- [x] `kano_requirements_offline_eval` の CLI black-box 実行で `status=ok` と golden 一致を確認している。
- [x] `kano_requirements_audit` の CLI black-box 実行で `status=ok` と `overall_assessment=no_go` を別々に確認している。
- [x] Code-to-gate 再実行で High/Critical effective finding が 0、または根拠付き follow-up がある。

## Evidence

- 初回懸念:
  - `kano_requirements_audit` fixture に `no_go` 要件があるが、現状の `overall_assessment` は `conditional_go` になり得る。
  - `kano_requirements_offline_eval` は `status=ok` だが、分類・昇格品質の golden Eval ではない。
- 実行コマンド:
  - `cd research-runtime && uv run pytest`
  - `uv run python -m rand_research.cli run-once --preset kano_requirements_offline_eval --max-items 5`
  - `uv run python -m rand_research.cli run-once --preset kano_requirements_audit --max-items 5`
  - `node C:\Users\ryo-n\Codex_dev\code-to-gate\dist\cli.js analyze C:\Users\ryo-n\Codex_dev\RanD --emit all --out C:\tmp\rand-code-to-gate-kano --llm-provider deterministic --cache force --ignore research-runtime/runs,research-runtime/state,research-runtime/.pytest_cache,research-runtime/src/rand_research_runtime.egg-info`
- テスト結果:
  - `uv run pytest`: 58 passed
  - `kano_requirements_offline_eval` run_id `20260528-141817-a36d15a4`: `status=ok`, promoted `REQ-001` / `KC-001` (must_be, P0, hard_gate), low-confidence attractive `KC-002` は `confidence below 0.7 threshold` で昇格拒否
  - `kano_requirements_audit` run_id `20260528-141925-a05fc0f1`: `status=ok`, `overall_assessment=no_go`, `go=1`, `conditional_go=1`, `no_go=1`
  - Code-to-gate run_id `ctg-202605281422`: Critical 0 / High 0 / Medium 0 / Low 0 / Suppressed 0
- 参照ドキュメント:
  - `docs/tasks/TASK-20260528-02-kano-eval-hardening.md`
  - `docs/evaluation.md`
  - `docs/acceptance/BB-20260528-02-kano-eval-hardening.md`

## Verification Result

- 判定: passed
- コメント:
  - `_build_gate_summary` の overall assessment ロジックを修正し、`no_go_count > 0` → `no_go` にした。
  - discovery candidate に `promotion_gate` を付与し、confidence閾値(>=0.7)、evidence tier(primary/user_signal)、kano_type(questionable除外)、拒否理由を gate artifact に残すようにした。
  - promoted candidate を先頭に並べ、promoted-only numbering で `REQ-001` / `KC-001` の drift を防いだ。
  - golden fixture を追加・強化し、discovery / audit の期待値、ID、priority、gate_policy、confidence、statement、evidence refs、rejection reasons を regression test している。
  - CLI black-box eval で `status=ok` と gate verdict の分離を確認している。
  - Code-to-gate で全 severity 0 を確認したため、fixture-based KanoMode Eval release gate は Go。
- フォローアップ:
  - live evidence を含む Kano pilot Eval と parallel state write hardening は別 Task Seed として扱う。

## Release Mapping

**Technical Acceptance**: 本 AC は技術検収 (technical acceptance) であり、KanoMode Eval の gate semantics・promotion gate・golden fixture の基準を満たしていることを確認する。

**Release Approval**: release approval は別途正式承認者による release gate で判定する。`release_approval_id` は空欄とする。

- Release Approval ID: (pending)
- Release Version: (pending)
- Release Note: (pending)
