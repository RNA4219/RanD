---
task_id: TASK-20260526-01-2
intent_id: INT-KANO-001
owner: rand
status: superseded
last_reviewed_at: 2026-05-28
next_review_due: null
depends_on: [TASK-20260526-01-1]
superseded_by: TASK-20260528-02
---

# Task Seed: KanoMode P0-2 - kano helper + artifact generation

## メタデータ

```yaml
task_id: TASK-20260526-01-2
repo: RanD
base_branch: main
work_branch: codex/kano-mode-p0-2
priority: P0
langs: [python, json]
```

## Objective

KanoMode の核心処理を実装する。`kano.py` helper を追加し、`kano.json` / `requirements_packet.json` artifact を生成する。

## Scope

- In:
  - `research-runtime/src/rand_research/kano.py`
  - `research-runtime/src/rand_research/fetchers.py` (kano_query_seed, kano_fixture_json)
  - `research-runtime/src/rand_research/reports.py` (extra_payloads)
  - `research-runtime/tests/fixtures/kano_evidence.json`
- Out:
  - tests + validation (P0-3)
  - peer repo API変更

## Requirements

- Behavior:
  - evidence cluster を組み立て、Kano 候補を生成する
  - persona-aware insight payload または deterministic fallback を生成する
  - `kano.json` を run directory に保存する
  - `requirements_packet.json` を保存する
  - confidence / bias_note / kill_condition 欠損候補は packet 昇格しない
- I/O Contract:
  - Input: evidence (fixture or query seed), preset metadata
  - Output: `kano.json`, `requirements_packet.json`
- Constraints:
  - schema_version を必須 field にする
  - 既存 preset の artifact 保存を破壊しない
- Acceptance Criteria:
  - `kano.json` が必須 root fields を持つ
  - `kano_candidates[*]` が必須 fields を持つ
  - `requirements_packet.json` が必須 root fields を持つ
  - `requirements[*]` が必須 fields を持つ

## Affected Paths

- `research-runtime/src/rand_research/kano.py`
- `research-runtime/src/rand_research/fetchers.py`
- `research-runtime/src/rand_research/reports.py`
- `research-runtime/tests/fixtures/kano_evidence.json`

## Local Commands

```powershell
cd research-runtime
uv run python -m unittest discover tests
uv run python -m rand_research.cli run-once --preset kano_requirements_offline_eval --max-items 5
```

## Deliverables

- helper: `kano.py`
- artifact: `kano.json`, `requirements_packet.json`
- fixture: `kano_evidence.json`

## Tests

- evidence cluster payload 生成
- artifact save
- validation (safety fields 昇格除外)

## Notes

### Rationale

核心処理を独立 seed にし、入口と検証を分離することで rollback 範囲を限定する。

### Risks

- evidence metadata schema 不整合
- validation logic 漏れ

### Follow-ups

- TASK-20260526-01-3: tests + validation + docs