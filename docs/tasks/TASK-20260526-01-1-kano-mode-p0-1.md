---
task_id: TASK-20260526-01-1
intent_id: INT-KANO-001
owner: rand
status: superseded
last_reviewed_at: 2026-05-28
next_review_due: null
depends_on: []
superseded_by: TASK-20260528-02
---

# Task Seed: KanoMode P0-1 - preset + prompt + pipeline入口

## メタデータ

```yaml
task_id: TASK-20260526-01-1
repo: RanD
base_branch: main
work_branch: codex/kano-mode-p0-1
priority: P0
langs: [python, markdown, json]
```

## Objective

KanoMode の実行入口を作る。preset と prompt を追加し、pipeline に `mode == "kano_requirements"` の最小分岐を追加する。

## Scope

- In:
  - `research-runtime/configs/presets/kano_requirements_hybrid.json`
  - `research-runtime/configs/presets/kano_requirements_offline_eval.json`
  - `research-runtime/prompts/kano_requirements_prompt.md`
  - `research-runtime/src/rand_research/pipeline.py` (最小分岐)
  - `docs/requirements_kano_mode.md` (既存)
  - `docs/specification_kano_mode.md` (既存)
- Out:
  - artifact generation (P0-2)
  - tests + validation (P0-3)
  - peer repo API変更

## Requirements

- Behavior:
  - `run-once --preset kano_requirements_hybrid` が受け付けられる
  - `run-once --preset kano_requirements_offline_eval` が受け付けられる
  - preset に `mode`, `topic`, `persona_modes`, `locales`, `insight_enabled` が宣言できる
- I/O Contract:
  - Input: preset JSON, topic, locale
  - Output: 既存 artifact（この段階では kano.json 未生成）
- Constraints:
  - 既存 preset の挙動を破壊しない
  - pipeline 分岐は最小（mode判定のみ）
- Acceptance Criteria:
  - preset JSON が存在し、必須キーを持つ
  - prompt ファイルが存在する
  - pipeline で `preset.mode == "kano_requirements"` の分岐が入る

## Affected Paths

- `research-runtime/configs/presets/*.json`
- `research-runtime/prompts/*`
- `research-runtime/src/rand_research/pipeline.py`

## Local Commands

```powershell
cd research-runtime
uv run python -m unittest discover tests
uv run python -m rand_research.cli heartbeat --dry-run --preset kano_requirements_offline_eval --max-items 2
```

## Deliverables

- preset: `kano_requirements_hybrid`, `kano_requirements_offline_eval`
- prompt: `kano_requirements_prompt.md`
- pipeline: mode分岐入口

## Tests

- 既存 unittest が通る
- preset load が成功する

## Notes

### Rationale

入口だけを先に確保し、artifact generation と validation を後続 seed で段階的に追加する。

### Risks

- preset schema 不整合で load 失敗

### Follow-ups

- TASK-20260526-01-2: kano helper + artifact generation
- TASK-20260526-01-3: tests + validation + docs