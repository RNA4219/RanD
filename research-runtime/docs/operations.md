# Operations

1. `env-check.ps1` で依存 repo を確認する
2. `run-once.ps1 -Preset paper_arxiv_ai_recent` で手動実行する
3. `runs/<run_id>/report.md` を読む
4. 必要なら `report.json`, `insight.json`, `gate.json`, `tracker_sync.json`, `memx_journal.json` を後段へ渡す
